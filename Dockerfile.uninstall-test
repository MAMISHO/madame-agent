FROM ubuntu:24.04

ENV PORT_OPENCODE=4098
ENV MADAME_PORT=3001
ENV NODE_VERSION=22.23.1
ENV HOME=/root
ENV PATH="/root/.opencode/bin:${PATH}"

RUN apt-get update && \
    apt-get install -y curl wget git build-essential sudo lsof && \
    curl -fsSL https://nodejs.org/dist/v${NODE_VERSION}/node-v${NODE_VERSION}-linux-x64.tar.gz | \
        tar -xzf - -C /usr/local --strip-components=1 && \
    curl -fsSL https://opencode.ai/install | bash && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

WORKDIR /root

RUN curl -fsSL https://raw.githubusercontent.com/MAMISHO/madame-agent/main/scripts/bootstrap.sh | bash

CMD ["opencode", "serve", "--port", "4098", "--hostname", "0.0.0.0"]